import HeaderTwo from "../components/common/HeaderTwo"
import BasePage from "./BasePage"

export default () => {
  return <BasePage title="Whoops! Not found!">
   <HeaderTwo>It doesn't look like there's anything here!</HeaderTwo>
  </BasePage>
}